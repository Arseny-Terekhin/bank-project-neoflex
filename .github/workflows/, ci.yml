name: CI Pipeline

on:
  push:
    branches: [ '**' ]
  pull_request:
    branches: [ '**' ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build and run tests with coverage
        run: mvn -B package jacoco:report

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./**/target/site/jacoco/jacoco.xml

      - name: Run SonarCloud analysis
        run: mvn -B org.sonarsource.scanner.maven:sonar-maven-plugin:3.11.0.2311:sonar -Dsonar.host.url=https://sonarcloud.io -Dsonar.projectKey=Arseny-Terekhin_bank-project-neoflex -Dsonar.organization=arseny-terekhin -Dsonar.token=${{ secrets.SONAR_TOKEN }}